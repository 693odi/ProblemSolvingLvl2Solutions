#include<bits/stdc++.h>
#include<string>
using namespace std;
//Welcome Mohammed :0)
#define fastio                        \
ios_base::sync_with_stdio(false); \
cin.tie(0);
#define ll long long
#define ld long double
#define rep(i, n) for (int i = 0; i < int(n); ++i)
#define repp(i, m, n) for (int i = m; i < int(n); ++i)
#define setp(n) << fixed << setprecision(n)
#define all(v) v.begin(),v.end()

void Lines() {
    cout<<"______________________________________________________________________________________\n";
}
void Numbersfrom1to10() {
    for (int i =1 ; i <= 10 ; i++) {
        cout<<i<<"\t\t";
    }
}
string Separator(int i ) {
    if (i < 10) {
        return "    |";
    }else {
        return "   |";
    }
}
void MultiplicationMSG() {
    cout<<"\n\n\t\t\t\t\t\t";
    cout<<"Multiplication Table From 1 to 10\n\n\t\t\t";
    Numbersfrom1to10();cout<<'\n';
    Lines();
}
void MulitplicationTableFrom1to10() {
    MultiplicationMSG();
    for (int i = 1; i<=10 ; i++) {
        cout<<"  "<<i<<Separator(i)<<"\t";
        for (int j = 1 ; j<= 10 ; j++) {
            cout<<i*j<<"\t\t";
        }
        cout<<'\n';
    }
}


int main() {
    srand((unsigned) time(NULL));
    ios_base::sync_with_stdio(0);
    MulitplicationTableFrom1to10();
    return 0;
}
