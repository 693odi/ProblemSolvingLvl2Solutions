#include<bits/stdc++.h>
#include<string>
using namespace std;
//Welcome Mohammed :0)
#define fastio                        \
ios_base::sync_with_stdio(false); \
cin.tie(0);
#define ll long long
#define ld long double
#define rep(i, n) for (int i = 0; i < int(n); ++i)
#define repp(i, m, n) for (int i = m; i < int(n); ++i)
#define setp(n) << fixed << setprecision(n)
#define all(v) v.begin(),v.end()

int ReadPositiveNumber(string msg) {
    int Number ;
    do {

        cout<<msg<<'\n';
        cin>>Number;

    }while (Number <= 0);
    return Number;
}
int RandomNumber(int From , int To) {
    return rand()%(To - From + 1) + From;
}
void BulidTheArray(int arr[100] , int& arrLength) {
    for (int i = 0 ; i < arrLength ; i++) {
        arr[i] = RandomNumber(1,100);
    }
}
void PrintTheArray(int arr[100] , int arrLength) {
    for (int i = 0 ; i < arrLength ; i++) {
        cout<<arr[i]<<" ";
    }
}
void SumArr1AndArr2(int arr1[100], int arr2[100], int arr3[100] , int arrLength ) {
    for (int i = 0 ; i < arrLength ; i++) {
        arr3[i] = arr1[i] + arr2[i];
    }
}

int main() {
    srand((unsigned) time(NULL));
    ios_base::sync_with_stdio(0);
    int arrLength , arr1[100] , arr2[100] , arrSum[100];
    arrLength = ReadPositiveNumber("How many elements ?\n");
    BulidTheArray(arr1,arrLength);
    BulidTheArray(arr2,arrLength);
    SumArr1AndArr2(arr1,arr2,arrSum,arrLength);
    cout<<"Array 1 Elements : \n";
    PrintTheArray(arr1,arrLength);
    cout<<"\nArray 2 Elements : \n";
    PrintTheArray(arr2,arrLength);
    cout<<"\nArray 3 After Sum Array 1 and Array 2 : \n";
    PrintTheArray(arrSum,arrLength);




    return 0;
}
