#include<bits/stdc++.h>
#include<string>
using namespace std;
//Welcome Mohammed :0)
#define fastio                        \
ios_base::sync_with_stdio(false); \
cin.tie(0);
#define ll long long
#define ld long double
#define rep(i, n) for (int i = 0; i < int(n); ++i)
#define repp(i, m, n) for (int i = m; i < int(n); ++i)
#define setp(n) << fixed << setprecision(n)
#define all(v) v.begin(),v.end()

int RandomNumber(int From , int To) {
    return rand()%(To - From + 1 ) + From;
}
void BuildTheArray(int arr[100], int& arrLength) {
    cout<<"How many elements ?\n";
    cin>>arrLength;
    for (int i = 0 ; i < arrLength ; i++) {
        arr[i] = RandomNumber(1,100);
    }
}
void CopyTheArrayReversedOrder(int arr1[100] , int arr2[100], int arrLength) {
    for (int i = 0 ; i < arrLength ; i++) {
        arr2[i] = arr1[arrLength - 1 - i ];
    }
}
void PrintTheArray(int arr[100], int arrLength) {
    for (int i = 0 ; i < arrLength ; i++)
        cout<<arr[i]<<" ";
    cout<<'\n';
}


int main() {
    srand((unsigned) time(NULL));
    ios_base::sync_with_stdio(0);
    int arr[100] , arr2[100] , arrLength;
    BuildTheArray(arr,arrLength);
    CopyTheArrayReversedOrder(arr,arr2,arrLength);
    cout<<"Array 1 elements : \n";
    PrintTheArray(arr,arrLength);
    cout<<"\n\nArray 2 after copying array 1 in reversed order: \n";
    PrintTheArray(arr2,arrLength);




    return 0;
}
