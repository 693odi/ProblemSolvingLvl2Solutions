#include<bits/stdc++.h>
#include<string>
using namespace std;
//Welcome Mohammed :0)
#define fastio                        \
ios_base::sync_with_stdio(false); \
cin.tie(0);
#define ll long long
#define ld long double
#define rep(i, n) for (int i = 0; i < int(n); ++i)
#define repp(i, m, n) for (int i = m; i < int(n); ++i)
#define setp(n) << fixed << setprecision(n)
#define all(v) v.begin(),v.end()

int RandomNumber(int From = 1 , int To = 100 ) {
    return rand()%(To - From + 1 )+ From;
}
int Sum(int array[100] , int arrLength) {
    int sum = 0;
    for (int i = 0 ; i < arrLength ;i++) {
       sum  = sum + array[i];
    }
    return sum;
}

void FillArrayRandomly(int array[100] , int& arrLength) {
    cout <<"Enter Array Size : ";
    cin>>arrLength;
    for (int i = 0 ; i < arrLength ; i++) {
        array[i] = RandomNumber(1,100);
    }
}
void PrintArrayElements(int array[100] , int arrLength) {
    cout<<"Array Elements : ";
    for (int i = 0 ; i < arrLength ; i++) {
        cout<<array[i]<<" ";
    }
}


int main() {
    srand((unsigned) time(NULL));
    ios_base::sync_with_stdio(0);
    int array[100] , arrLength;
    FillArrayRandomly(array,arrLength);
    PrintArrayElements(array,arrLength);
    cout<<"\nSum of all number is : "<<Sum(array,arrLength);




    return 0;
}
