#include<bits/stdc++.h>
#include<string>
using namespace std;
//Welcome Mohammed :0)
#define fastio                        \
ios_base::sync_with_stdio(false); \
cin.tie(0);
#define ll long long
#define ld long double
#define rep(i, n) for (int i = 0; i < int(n); ++i)
#define repp(i, m, n) for (int i = m; i < int(n); ++i)
#define setp(n) << fixed << setprecision(n)
#define all(v) v.begin(),v.end()

int RandomNumber(int From , int To) {
    return rand()%(To - From + 1 ) + From;
}
int ReadPositiveNumber(string msg) {
    int Number ;
    do {
        cout<<msg<<'\n';
        cin>>Number;
    }while (Number<=0);
    return Number;
}
void BuildTheArray(int arr[100] , int& arrLength) {
    cout<<"How many numbers?\n";
    cin>>arrLength;
    for (int i = 0 ; i < arrLength; i++) {
        arr[i] = RandomNumber(1,100);
    }
}
bool FindNumberPosition(int arr[100], int arrLength , int Goal) {
    for (int i = 0 ; i < arrLength ;i++) {
        if (arr[i] == Goal) {
            return true;
        }
    }
    return false;
}
void PrintTheArray(int arr[100], int arrLength) {
    for (int i = 0 ; i < arrLength ; i++) {
        cout<<arr[i]<<" ";
    }
}



int main() {
    srand((unsigned) time(NULL));
    ios_base::sync_with_stdio(0);
    int arr[100], arrLength ;
    BuildTheArray(arr,arrLength);
    cout<<"Array elements : \n";
    PrintTheArray(arr,arrLength);
    int Goal = ReadPositiveNumber("\nPlease enter a number to search for ?");
    cout<<"Number you are looking for is "<<Goal<<'\n';
    if (FindNumberPosition(arr,arrLength,Goal)) {
        cout<<"Yes ,the number is found :-)\n";
    }else {
        cout<<"No , the number is not found :-(\n";
    }








    return 0;
}
